apiVersion: v1
kind: Service
metadata:
  name: {{ include "flaggy.fullname" . }}
  labels: {{- include "flaggy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "flaggy.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
